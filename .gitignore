# Secrets — never commit credentials or API tokens
.env
.env.*
!.env.example

# Local acquisition workspace (large raw AIS downloads stay out of git)
data/raw/

# Node
node_modules
dist
dist-ssr

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
